import React from 'react';
import styled from 'styled-components';

interface IToday {}

export default function Today({}: IToday) {
  return <S.Today>Today</S.Today>;
}

const S = {
  Today: styled.div``,
};
