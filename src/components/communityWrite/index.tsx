import React from 'react';
import styled from 'styled-components';

export default function CommunityWrite() {
  return <S.communityWrite>communityWrite</S.communityWrite>;
}

const S = {
  communityWrite: styled.div``,
};
