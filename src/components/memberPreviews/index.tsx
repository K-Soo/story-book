import styled from 'styled-components';

interface IMemberPreviews {}

export default function MemberPreviews({}: IMemberPreviews) {
  return <S.MemberPreviews>MemberPreviews</S.MemberPreviews>;
}

const S = {
  MemberPreviews: styled.div``,
};
