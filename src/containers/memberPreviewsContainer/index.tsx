import MemberPreviews from '@components/memberPreviews';

interface IMemberPreviewsContainer {}

export default function MemberPreviewsContainer({}: IMemberPreviewsContainer) {
  return <MemberPreviews />;
}
