import styled from 'styled-components';

interface ILibraryContainer {}

export default function LibraryContainer({}: ILibraryContainer) {
  return <S.LibraryContainer>LibraryContainer</S.LibraryContainer>;
}

const S = {
  LibraryContainer: styled.div``,
};
