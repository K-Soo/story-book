import styled from 'styled-components';
import PreviewList from '@components/member/previewList';

interface IPreviewListContainer {}

export default function PreviewListContainer({}: IPreviewListContainer) {
  return <PreviewList />;
}
